#include <stdio.h>
#include <ctype.h>
/**
 * main - Entry point
 * Desription: Print the given instructions
 * Return: 0
*/
int main(void)
{
char c;

for (c = 'a'; c <= 'z'; c++)
	putchar(c);
	putchar('\n');

return (0);
}

